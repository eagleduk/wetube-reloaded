extends ./base.pug

block content
    if errorMessage 
        span= errorMessage
    form(method="POST")
        input(type="text", name="email", placeholder="Email", required)
        input(type="password", name="password", placeholder="Password", required)
        input(type="submit", value="Login")
        a(href="github") Countinue with Github &rarr;